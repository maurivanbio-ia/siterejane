// Configuração das estatísticas de acesso.
// Enquanto os dois campos estiverem vazios, o site não registra nada
// e o painel (admin.html) mostra dados de demonstração.
// Para ativar: crie um projeto em supabase.com, rode supabase-schema.sql
// e preencha abaixo a "Project URL" e a "anon public key" (Settings → API).
window.RR_ANALYTICS = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
